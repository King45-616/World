//PAsso: Colocar o valor total de maquinas
//Passo: Colocar o valor de maquinas defeituosas
//PAsso: Calcular a porcentagem de maquinas defeituosas usando a formula "% = (Defeituosas/total de maquinas) * 100
//PAsso: Ver aonde a porcentagem se encaixa
//Passo: Se for maior que 10%, precisa de conserto 
//PAsso: Se for menor que 10%, não precisa de conserto

#include <stdio.h>

int main() {
  float maquinas;
  float defeituosas;
  printf("digite o total de maquinas: ");
  scanf("%f", &maquinas);
  printf("digite o total de maquinas defeituosas: ");
  scanf("%f", &defeituosas);
  float porcentagem = (defeituosas / maquinas) * 100;
  printf("Porcentagem sera igual a %f, ", porcentagem);
  if(porcentagem <= 10) {
    printf(" nao precisa de conserto");
  } else {
    printf("precisa de conserto");
  }
  return 0;
}
