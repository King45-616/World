#include <stdio.h>

 int main(void) {
    int km = 600;
    int litros= 100;
    int consumo = 600 / 100;
    printf("o consumo medio do veiculo sera %d por %d igual a %d", km, litros, consumo);

    return 0;
 }
